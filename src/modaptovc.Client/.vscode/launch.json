{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "edge",
      "request": "launch",
      "name": "localhost (Edge)",
      "url": "http://localhost:55083",
      "webRoot": "${workspaceFolder}",
      "runtimeArgs": [ "--disable-web-security" ]
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "localhost (Chrome)",
      "url": "http://localhost:55083",
      "webRoot": "${workspaceFolder}",
    }
  ]
}